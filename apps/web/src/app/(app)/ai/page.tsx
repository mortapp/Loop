import { PlaceholderScreen } from "../placeholder-screen";

export default function AiPage() {
  return (
    <PlaceholderScreen
      title="AI"
      description="This will host LOOP's assistant: a tool registry over your data plus a confirmation step before any action (sending a quote, filing a return, publishing a listing) actually runs."
    />
  );
}
