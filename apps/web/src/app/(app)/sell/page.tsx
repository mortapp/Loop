import { PlaceholderScreen } from "../placeholder-screen";

export default function SellPage() {
  return (
    <PlaceholderScreen
      title="Sell"
      description="RECOVER / ResellLens. This will list your owned items with valuations, let you draft listings, and track them through to a sale — public.valuations, public.listings, public.sales."
    />
  );
}
