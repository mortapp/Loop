import { PlaceholderScreen } from "../placeholder-screen";

export default function MoneyPage() {
  return (
    <PlaceholderScreen
      title="Money"
      description="This will render the public.money_events ledger: everything earned via MAKE, spent on purchases, refunded via PROTECT, and recovered via RECOVER, in one running total."
    />
  );
}
