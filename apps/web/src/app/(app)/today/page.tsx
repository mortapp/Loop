import { PlaceholderScreen } from "../placeholder-screen";

export default function TodayPage() {
  return (
    <PlaceholderScreen
      title="Today"
      description="This will surface the unified public.actions queue: quotes to follow up on, returns nearing their window, warranties about to expire, and anything else LOOP thinks you should act on right now."
    />
  );
}
