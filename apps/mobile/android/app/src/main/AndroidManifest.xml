<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Real network access for release builds: Supabase auth/data/storage
         calls, the Ask LOOP AI backend, item-photo upload. debug/profile
         manifests already carry this (stock Flutter template default,
         added there only for the dev-tools hot-reload channel), but a
         release build only merges main/AndroidManifest.xml, so without
         this here a real release APK would silently fail every network
         call. Found during the iOS parity audit: iOS needs no equivalent
         declaration, since Apple doesn't gate outbound network access
         behind a manifest-style permission the way Android does. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:label="LOOP"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Supabase Auth OAuth and email-confirmation callbacks land
                 here. The exact URI is centralized in
                 lib/core/auth/mobile_auth_contract.dart and allow-listed
                 in Supabase. Supabase Flutter owns the PKCE code exchange;
                 this activity only receives the approved deep link. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="com.loop.app.loop-mobile" android:host="app" android:path="/login-callback"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
